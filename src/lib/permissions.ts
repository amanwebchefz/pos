// Permission name constants (for reference only - actual permissions come from backend)
// All permission checks should use string literals like 'products.create' instead of constants

export type Permission = string;

